org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
android.enableJetifier=true
# Pin Gradle to Android Studio's bundled JDK 21 so Flutter CLI and Android Studio use the same JVM.
# Without this, Homebrew's Java 25 gets picked up from PATH and breaks the Android Gradle Plugin.
org.gradle.java.home=/Users/beingadish/Applications/Android Studio.app/Contents/jbr/Contents/Home
